source "$DOTFILES/zsh/options/directories.sh"
source "$DOTFILES/zsh/options/history.sh"
source "$DOTFILES/zsh/options/corrections.sh"

# Set prompt substitution so we can use the vcs_info_message variable
setopt prompt_subst
