#####################################
## Directories                     ##
#####################################

# Auto cd into a command if it is a directory
setopt auto_cd

# Auto push the directory stack if a command is run
setopt auto_pushd

# Make sure a directoty is added to the stack only once
setopt pushd_ignore_dups

